#!/usr/bin/env node

const { execSync } = require('child_process');

try {
  // `stdin`, `stdout`, `stderr`
  execSync('npx @million/install@latest', {
    stdio: ['inherit', 'inherit', 'ignore'],
  });
} catch (_) {
  /**/
}
